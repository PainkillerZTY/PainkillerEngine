 // Math stub — this library is header-only via GLM wrappers.
 // This file exists so CMake OBJECT libraries don't complain about
 // missing source files.
 #include "Math.h"
 
 namespace painkiller {
     // Intentionally empty — all math is inline/header-only.
 }
